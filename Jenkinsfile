pipeline {

    agent any

    stages {

        stage("build") {

            steps {
                echo 'building started'
            }
        }

        stage("test") {

            steps {
                echo 'test started'
            }
        }

        stage("deploys") {

            steps {
                echo 'deploy started'
            }
        }

        stage("email") {

            steps {
                echo 'sending email'
                
            }
        }
    }
}